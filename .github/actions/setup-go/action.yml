name: 'Setup Go with Caching'
description: 'Checks out the repository and sets up Go with dependency caching'

runs:
  using: "composite"
  steps:      
    - name: Set up Go
      uses: actions/setup-go@v5
      with:
        go-version-file: go.mod
        cache-dependency-path: go.sum
        cache: true